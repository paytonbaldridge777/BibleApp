import type { NextConfig } from "next";

// TODO [ADAPTER-MIGRATION]: @cloudflare/next-on-pages is deprecated as of 2026.
// Cloudflare recommends migrating to the OpenNext adapter: https://opennext.js.org/cloudflare
// Build command will change from: npx @cloudflare/next-on-pages@1
// To: npx opennextjs-cloudflare@latest
// See DEPLOYMENT.md for current build settings. Address this before next major Next.js upgrade.

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
